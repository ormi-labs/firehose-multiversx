start:
  args:
  - firehose
  - merger
  - reader-node
  - relayer
  flags:
    # Specifies the path to the binary
    reader-node-path: "node/node"
    # Flags that will be added to the dummy chain process command
    reader-node-arguments: --firehose-enabled
